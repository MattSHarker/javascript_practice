// FUNCTIONS
// functions are a small piece of code wrapped inside a value
let x = 20;
console.log("the value of x is: ", x);  // prints the contents inside () to the console

// RETURN VALUES
// some functions will return a value when they finish execution
let max = Math.max(3, 6);   // this returns the higher of the two values 
                            // and it gets stored in the variable "max"

// for creating functions, refer to __________
